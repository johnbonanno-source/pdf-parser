# Copy to `.streamlit/secrets.toml` for local dev (do not commit real secrets).
# In deploy, set this via your platform's secrets manager / environment.
DATABASE_URL = "postgresql+psycopg://protocols_user:change_me@localhost:5432/protocols"
