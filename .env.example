# Local dev (docker compose)
POSTGRES_DB=protocols
POSTGRES_USER=protocols_user
POSTGRES_PASSWORD=change_me
POSTGRES_PORT=5432

# App connection string (SQLAlchemy + psycopg)
DATABASE_URL=postgresql+psycopg://protocols_user:change_me@localhost:5432/protocols
